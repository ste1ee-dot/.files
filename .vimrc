set tabstop=2      " The width of a tab character
set shiftwidth=2   " The width of auto-indents (e.g., using << or >>)
set softtabstop=2  " Fine-tunes spaces when hitting Tab or Backspace
set expandtab  
set clipboard=unnamedplus
set autoindent
